{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "Manage user tasks",
  "main": ".",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start" : "concurrently \"cd client && npm run dev\" \"cd server && npx nodemon index.js\""
  },
  "author": "VikasPattar",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
